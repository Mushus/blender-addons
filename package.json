{
  "name": "blender-addons",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "zip": "uv run python scripts/make_zip.py",
    "sync": "rulesync generate",
    "test": "echo \"Error: no test specified\" && exit 1",
    "site:dev": "pnpm --filter site dev",
    "site:build": "pnpm --filter site build",
    "site:generate-packages": "pnpm --filter site generate:packages",
    "site:preview": "pnpm --filter site preview",
    "site:lint": "pnpm --filter site lint",
    "site:format": "pnpm --filter site format",
    "site:format:check": "pnpm --filter site format:check"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "^11.20.0",
      "onFail": "download"
    }
  },
  "type": "module",
  "dependencies": {
    "rulesync": "^16.7.0"
  }
}