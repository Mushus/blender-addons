{
  "name": "blender-addons",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "sync": "rulesync generate",
    "test": "echo \"Error: no test specified\" && exit 1",
    "site:dev": "uv run python scripts/generate_pages.py && pnpm --filter site dev",
    "site:build": "uv run python scripts/generate_pages.py && pnpm --filter site build",
    "site:preview": "pnpm --filter site preview",
    "site:lint": "pnpm --filter site lint",
    "site:format": "pnpm --filter site format",
    "site:format:check": "pnpm --filter site format:check"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "^11.20.0",
      "onFail": "download"
    }
  },
  "type": "module",
  "dependencies": {
    "rulesync": "^16.7.0"
  }
}